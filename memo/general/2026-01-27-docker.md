---
layout: memo
title: Docker tips
---

# Pass secrets in Dockerfile
Setting secrets in a Dockerfile using `ENV` or `ARG` is [insecure](https://docs.docker.com/reference/build-checks/secrets-used-in-arg-or-env/).

[Build secrets](https://docs.docker.com/build/building/secrets/) should be used instead.
